pipeline{
    agent { docker {
      image 'python:3.7.2'
      args  '--net="host"'
     } }
    stages{
        stage('build') {
            steps {
                sh 'sudo pip install redis'

            }
        }
    }



}
