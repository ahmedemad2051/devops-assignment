pipeline {
   agent any
   stages {
       stage('Build') {
           agent {
               docker {
                   image 'python:3.6.8'
                   args  '--net="host" --user="root"'
               }
           }
           steps {
              sh 'pip3 install -r app/requirements.txt --user'
           }
       }
       stage("test"){
           agent {
               docker {
                   image 'python:3.6.8'
                   args  '--net="host" --user="root"'
               }
           }
          steps{
              sh 'python app/tests/test.py'
          }
          post{
              success{
                  echo "======== test successfully========"
              }
              failure{
                  echo "======== test failed========"
              }
          }
      }
      stage('Publish') {
          steps{
              script {
                  def dockerImage = docker.build("app" + ":$BUILD_NUMBER","--net host -f app/Dockerfile .")
              }
          }
      }




   }
}
