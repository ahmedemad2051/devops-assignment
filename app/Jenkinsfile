pipeline {
   agent any
   stages {
       stage('Build') {
           agent {
               docker {
                   image 'python:3.6.8'
                   args  '--net="host" --user="root"'
               }
           }
           steps {
              sh 'pip3 install -r app/requirements.txt --user'
           }
       }




   }
}
