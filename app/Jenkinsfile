pipeline {
   agent any
   stages {
       stage('Build') {
           agent {
               docker {
                   image 'golang'
               }
           }
           steps {
               sh 'cd app && go build'              
           }
       }




   }
}
