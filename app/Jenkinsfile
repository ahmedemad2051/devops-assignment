pipeline{
    agent { docker {
      image 'python:3.7.2'
      args '-v $HOME/src:/src'
     } }
    stages{
        stage('build') {
            steps {
              dir(path: '/src') {
                sh 'pip install -r requirements.txt'
              }
            }
        }
    }



}
